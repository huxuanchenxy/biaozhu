# 2026-09-04

## 路由模式改为 hash（承接 09-03 脚手架）

- 起因：用户访问 http://localhost:3001/home 发现 URL 没有 `#`，希望改成 hash 模式
- 改动：
  - `src/router/index.ts`：`createWebHistory` → 按 `VITE_ROUTER_MODE` 在 `createWebHashHistory` / `createWebHistory` 间切换
  - `.env` 新增 `VITE_ROUTER_MODE = hash`（默认 hash）
  - `env.d.ts` 补 `VITE_ROUTER_MODE` 类型
  - README 增加「路由模式」「端口」两节
- 验证：`npm run build` 通过；dev 启动正常，`/` 与 `/home` 均返回 200
- 注意：改 `.env` 后必须重启 dev server 才生效（env 在启动时加载，HMR 不会重新读）

## 新增 Markdown 标注页面（/markdown）

- 需求：保留平台抬头、不要「首页/关于」tab，页面左 2/3 / 右 1/3
- 新增文件：`src/views/MarkdownAnnotatorView.vue`、`src/utils/markdown.ts`
- 改动：
  - `src/router/index.ts` 注册 `/markdown`，用 `meta: { hideNav: true }` 控制隐藏导航
  - `src/App.vue`：`hideNav` 为 true 时隐藏 `el-menu`、给 `el-main` 加 `--flush`（padding 0 + overflow hidden），logo 与环境标签保留
  - 左栏：编辑 / 分栏 / 预览三态，textarea + markdown-it 渲染；右栏：划选文本 → 选标签 → 添加标注 → 预览区高亮 + 列表管理 + 导出 JSON
- 依赖：装了 `markdown-it` v15（自带 TS 类型，已卸掉多余 `@types/markdown-it`）
- 高亮实现：先 `unwrapMarks` 还原 DOM，再用 TreeWalker 找文本节点、`Range.surroundContents` 包 `<mark class="ann" data-id>`；每条标注只高亮首次出现，已包过的节点跳过
- 验证：`vue-tsc -b` 与 `vite build` 均通过

## 环境坑：Vite 缓存目录删除失败导致 dev 起不来

- 现象：`npm install` 后首次 `vite` 报 `[safe-delete] 操作失败 ... node_modules\.vite\deps`
- 原因：Vite 要删 `node_modules/.vite/deps` 重建预构建缓存，沙箱的 safe-delete shim（genie-trash）对该路径失败
- 解决：`mv node_modules/.vite node_modules/.vite_bak_<时间戳>` 改名绕过（不要用 rm），Vite 会重新生成

## 环境情况

- 本机 3000、3001 端口当时都被其他 node 进程占用，Vite 会自动顺延到 3002，启动日志会写明实际端口
- 用户当时跑的是 3001 上的实例；我做冒烟测试起的 3002 实例已用 Stop-Process 清理，3000/3001 未动
