# 2026-09-03

## 从零搭建 Vue3 前端脚手架（G:\project\biaozhu）

- 技术选型（用户确认）：TypeScript + Element Plus + npm，接口沿用 http://10.89.34.77:8080
- 技术栈版本：vue 3.5 / vue-router 4.5 / axios 1.20 / element-plus 2.9 / vite 6.4 / typescript 5.7 / vue-tsc 2.2，Node 22.22.2
- 目录：src/{api,router,utils,styles,views}，路径别名 `@` -> src（vite.config.ts 与 tsconfig.app.json 双处配置）
- 环境变量分三档：.env（通用）/ .env.development / .env.production
  - VITE_APP_TITLE / VITE_APP_ENV / VITE_PORT=3000 / VITE_APP_BASE_API=/api / VITE_PROXY_TARGET / VITE_OUT_DIR
  - env.d.ts 里补了 ImportMetaEnv 类型提示
- HTTP 模块 src/utils/request.ts：axios 实例 + 请求/响应拦截器 + 统一中文报错（含 4xx/5xx 映射、超时、断网）；
  业务码成功判定 SUCCESS_CODES=[0,200]；后端裸返回数组/对象时自动跳过 code 校验原样透传；
  封装 get/post/put/delete/upload/download 六个方法
- 踩坑：axios 1.20 的响应泛型改为 `Promise<AxiosResponseResult<T,R,D,P>>`，`service.get<any, T>()` 无法直接赋给 `Promise<T>`。
  解决：统一写成 `service.get<any, any>(url, config) as Promise<T>`
- 验证结果：npm run build 通过（elementPlus chunk 1.09MB / gzip 341KB，全量引入）；npm run dev 首页 200，/api/session/list 经代理返回 200
- 未包含（用户需要时再加）：Pinia、ESLint/Prettier、登录鉴权、Element Plus 按需引入

## 待办 / 可选项

- Element Plus 目前全量引入，体积偏大，可切 unplugin-vue-components + ElementPlusResolver 按需
- 后续可加 .env.staging（配 `vite build --mode staging`）
