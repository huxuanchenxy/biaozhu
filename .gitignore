node_modules
dist
dist-ssr
.DS_Store
*.local

# 日志
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# 构建缓存
*.tsbuildinfo

# 编辑器
.vscode/*
!.vscode/extensions.json
.idea

# 本地环境变量（含密钥的放这里，不要提交）
.env.local
.env.*.local

# 自托管的翻译模型与 wasm 运行时（体积大，由 `npm run fetch:model` 生成，不入库）
public/models
public/wasm
